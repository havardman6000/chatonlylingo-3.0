// src/lib/init.ts

// Export utility functions
export const isTokenManagerInitialized = (): boolean => {
  return false; // Always return false as tokenManager is removed
};

export const getTokenManager = (): null => {
  return null; // Always return null as tokenManager is removed
};

export const getEthereum = () => {
  return null; // Always return null as ethereum is removed
};

export const getWeb3 = (): null => {
  return null; // Always return null as Web3 is removed
};
